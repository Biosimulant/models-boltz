# BioNeMo result acceptance Lab

Validate captured NVIDIA responses, enforce candidate coverage and preserve a reusable experiment through MCP.

See MODEL_COMPATIBILITY.md. Empty default records intentionally fail acceptance. Populate them only with actual captured responses or explicitly labelled software-test fixtures.
